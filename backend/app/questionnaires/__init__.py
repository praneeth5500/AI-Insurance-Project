"""The adaptive questionnaire.

A question *definition* is data, not code: one renderer walks a versioned list
(docs/03_FRONTEND_ARCHITECTURE.md section 3, "Do not hard-code every question
as a separate unique component").
"""
