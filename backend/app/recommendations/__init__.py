"""Matched options.

Phase 5 builds the *experience* on synthetic products so the UX can be
validated before real data exists. The matching engine described in
docs/06_RECOMMENDATION_ENGINE.md — hard eligibility, fit evaluators, priority
weighting, scoring versions, immutable runs — is Phase 9.
"""
